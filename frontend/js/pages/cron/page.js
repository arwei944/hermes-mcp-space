/**
 * 定时任务页面 - 页面骨架
 */

const CronPageLayout = (() => {
    function buildLayout() {
        return `<div id="cron-list"></div>`;
    }

    return { buildLayout };
})();

export default CronPageLayout;
