/**
 * 系统配置页面 - 页面骨架
 */

const ConfigPageLayout = (() => {
    function buildLayout() {
        return `
            <div id="config-form"></div>
        `;
    }

    return { buildLayout };
})();

export default ConfigPageLayout;
