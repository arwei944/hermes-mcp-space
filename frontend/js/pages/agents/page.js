const AgentsPageLayout = (() => {
    function buildLayout() {
        return `
            <div id="agents-list"></div>
        `;
    }

    return { buildLayout };
})();

export default AgentsPageLayout;
